# programa que ao digitar no teclado um numero quebrado ele irá mostar a parte inteira
from math import floor

num = float(input('Digite um número quebrado para separar sua parte inteira:\n'))

print(floor(num))